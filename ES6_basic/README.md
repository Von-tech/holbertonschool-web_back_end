# ES6 Basics

This project demonstrates the use of `const` and `let` in JavaScript, and tests the functions using Jest.

## Installation

1. Clone the repository.
2. Install dependencies:

   ```bash
   npm install

Run the code:  npm run dev 0-main.js

